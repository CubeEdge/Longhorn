# 🖥️ Longhorn Server (后端服务器)

Longhorn 网络存储系统的核心 API 服务器。

## 技术栈
- **运行环境**: Node.js
- **框架**: Express
- **数据库**: SQLite3 (存放在根目录 `longhorn.db`)
- **文件系统**: 模拟分布式存储 (DISK_A)

## 核心 API 端点
- `/api/auth`: 登录与认证
- `/api/files`: 文件列出、重命名、移动、删除
- `/api/files/upload`: 分片上传处理
- `/api/share`: 外部公开分享链接
- `/api/user/stats`: 仪表盘统计数据

## 配置
配置文件详见根目录的 `ecosystem.config.js` (PM2 配置)。

## 启动
```bash
# 进入目录
cd server

# 安装依赖
npm install

# 启动 (PM2)
pm2 start ecosystem.config.js
```
