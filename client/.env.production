# Production Environment Variables

# Upload Base URL - Set to a subdomain that bypasses Cloudflare proxy
# to avoid 100-second timeout on large file uploads.
# Example: https://upload.opware.kineraw.com
# Leave empty to use the same domain as the app.
VITE_UPLOAD_BASE_URL=
