- [x] **账单与结案渲染维稳**: 修复 PI 发票与维修报告详情预览在费用计算与空值防御上的白屏级 Bug，并在发票界面使用准确的商业术语 (v12.3.168)
# Longhorn 开发任务 backlog

> **最后更新**: 2026-03-11
> **版本**: 12.3.169

---

## 进行中任务

### [SVC-P2] Service P2 详情页与稳定性补齐 (已完成 100%)
- [x] **组件联动与视窗管理**: 支持从工单详情页点击关联工单时智能开启新窗，规避流程丢失 (v12.3.169)
- [x] **台账字号与降噪梳理**: 改善并降级了产品台账信息栏的标题和字段尺寸，屏蔽布尔值渲染溢出及场景内冗余徽章 (v12.3.159)
- [x] **无缝编辑**: 产品台账详情页支持原地编辑，保存后自动刷新 (v12.3.158)
- [x] **分阶段渲染与防抖保存**: 重构 `RepairReportEditor` 支持 OP/MS 场景拆卸与 5s 自动保存引擎 (v12.3.158)
- [x] 重构产品/SKU管理列表至详情页的导航流
- [x] **统计数字防穿透**: 修复了联保关联工单展板的数据总数与实拉卡片数无法匹配的问题 (v12.3.151)
- [x] **语义与色彩标定**: 将产品卡片、工单详情联保卡片全部修正为一致的判定体系（无视工单侧 `is_warranty`，只看产品本身实际算出的 Active 还是 Expired），并通过统一的`保内(绿)/过保(红)`规范显示。(v12.3.151)
- [x] 精修各级详情页字号层级，对齐全局视觉
- [x] 修复 `ProductWarrantyRegistrationModal` 关键运行时 Bug
- [x] 调整 Backend 路由鉴权逻辑，支持 MS 部门只读视图
- [x] **服务成果管理卡片精简**: 将诊断、维修、结算报表整合为统一的 Service Center 卡片 (v12.3.146)
- [x] **产品/实物档案融合**: 将 SN、保修、历史工单合并至工单头部基本信息 (v12.3.147)
- [x] **智能型号校验**: 增加型号不符警示与“一键修正”逻辑，并已修复由于默认传参导致的错误指向绑定 (v12.3.153)
- [x] **统一保修引擎校验与溯源**: 所有组件独立页面或业务嵌入页统一通过 `warranty API` 获取保修引擎的逻辑判定结果，并在前端允许溯源展现出证起点与终点 (v12.3.153)
- **跨组件全局降压**: 削减多余巨型标题和计数器字号，提升信息排布密度 (v12.3.153)
- [x] **成果中心内容下钻**: 点击“报告提交”活动轴或 Service Center 按钮，弹出侧滑窗口展示结构化诊断/维修细节 (v12.3.148)
- [x] **服务详情抽屉增强**: `ActivityDetailDrawer` 支持配件预估列表渲染与维修老化描述展示 (v12.3.148)
- [x] **UI 视觉抛光**: 产品型号/SN 一行显示；警示框左对齐；时间轴记录单行紧凑化 (v12.3.149)
- [x] **资产联保重构**: 修改 `UnifiedTicketDetail` 和 `ProductCard` 保修状态UI为中文“在保内”与“已过保” (v12.3.150)
- [x] **复用对象关联面板**: 将产品档案中的服务历史重构为互动型「打卡」折叠展板，实现关联维修记录的预览下钻 (v12.3.150)
- [x] 版本发布：v12.3.153 已全量差量部署至 mini

### [SYSTEM-002] 系统环境同步与部署校验 (已完成)
- [x] 执行 `/context` 同步最新 OPS 规范
- [x] 执行 `/upd` 推进版本至 v12.3.38 (Client) / v1.7.31 (Server)
- [x] 远程 PM2 重载校验成功

### [SVC-P2] Service P2 架构升级 (已完成 100%)
**Phase 2.6: 高级协作与部门池** ✅
- [x] 工作区三视图逻辑闭环 (服务端过滤)
- [x] 侧边栏动态角标 (My Tasks/Mentioned/Team Queue)
- [x] 部门感知的任务分配与领取逻辑 (Pick up)
- [x] 自动参与者关联机制 (指派自动加入协作)
- [x] 版本发布：v12.3.13 已全量部署至 mini
**Phase 2.1: 数据基础** ✅
- [x] 数据库迁移：020_p2_unified_tickets.sql, 021_migrate_tickets_data.js
- [x] SLA 引擎：sla_service.js (时长矩阵/状态检测/超时计数)
- [x] 保修计算引擎：warranty_service.js
- [x] 向后兼容 API 适配层：legacy-adapter.js

**Phase 2.2: 权限与协作** ✅
- [x] 穿透式权限中间件：middleware/permission.js
- [x] @Mention 解析与通知：ticket-activities.js
- [x] participants 管理 API + 活动时间轴 API

**Phase 2.3: 前端重构** ✅
- [x] Workspace 组件群：WorkspaceComponents.tsx
- [x] 工单详情页增强：TicketDetailComponents.tsx
- [x] Overview 仪表盘：OverviewDashboard.tsx
- [x] View As 权限组件：ViewAsComponents.tsx
- [x] 导航架构重构：ServiceNavigation.tsx (独立模块化导航)

**Phase 2.4: 验证与调优** ✅
- [x] 数据迁移验证：本地 43 条 / 远程 48 条
- [x] SLA 验证脚本：scripts/validate_sla.js
- [x] 权限场景测试：scripts/test_view_as.js (93.8% 通过率)
- [x] 性能优化索引

### [WIKI-006] 知识库检索与翻译精度优化 (待开始)
- [ ] 针对长篇技术文档的章节化摘要生成
- [ ] 优化 PDF 导入时的表格排版

---

## 📅 最近更新历史 (From 1_Backlog.md)
 
 ### 2026-03-11 (成果详情下钻与侧滑交互闭环)
 - **成果内容下钻 (Outcome Insight) (v12.3.148)**:
     - **侧滑抽屉重构**: 为 `ActivityDetailDrawer` 配置了诊断报告与维修记录的专有渲染模板，支持配件、工时及判定细节的结构化展示。
     - **交互点对点联通**: 实现了 Service Center 按钮与历史活动的精准锚定与弹出。
     - **版本部署**: 递增版本并执行 `/upd` 全量远程同步。

### 2026-03-11 (服务成果整合与版本发布)
- **服务中心 (Service Center) 统一化 (v12.3.146)**:
    - **UI 整合**: 将诊断概况、维修执行、结算报表三个大卡片合并为一个紧凑卡片。
    - **交互设计**: 采用 2 列网格布局，支持状态感知按钮（有数据高亮，无数据禁用）。
    - **权限控制**: 底部商务结算区仅对特权用户开放。
    - **版本部署**: 递增版本并执行 `/upd` 全量远程同步。

### 2026-03-11 (产线目录详情化与权限增强)
 UI 优化与数据库修复)
- **侧边栏与成果中心优化 (v12.3.144)**:
    - **UI 排版**: 移除侧边栏冗余标题，修复 `padding/overflow` 问题及卡片间空白。
    - **服务成果中心**: 增大标题 (15px) 和描述 (13px) 字号，实现无数据时的“暂无报告”按钮禁用状态。
    - **快速注册流**: 实现“录入新产品”在原地弹出注册弹窗，并支持序列号和机型的智能自动带入。
    - **数据库修复**: 补齐了 `products` 表缺失的 17 个关键保修字段，解决了注册接口的 500 报错。
    - **部署**: 执行了全量构建与线上 `mini` 自动更新流程。

### 2026-03-10 (版本迭代与快速部署)
- **交付流水线更新 (v12.3.139)**:
    - **版本号对齐**: 同步更新 Client (`12.3.139`) 和 Server (`1.8.10`) 版本号。
    - **构建验证**: 完成本地 `npm run build` 验证。
    - **快速部署**: 执行 `./scripts/deploy.sh` 完成对 `mini` 服务器的差量同步与服务热重载。

- **交付流水线更新 (v12.3.138)**:
    - **版本号对齐**: 同步更新 Client (`12.3.138`) 和 Server (`1.8.9`) 版本号。
    - **构建验证**: 完成本地 `npm run build` 验证。
    - **快速部署**: 执行 `./scripts/deploy.sh` 完成对 `mini` 服务器的差量同步与服务热重载。

- **交付流水线更新 (v12.3.137)**:
    - **版本号对齐**: 同步更新 Client (`12.3.137`) 和 Server (`1.8.8`) 版本号。
    - **构建验证**: 完成本地 `npm run build` 验证。
    - **快速部署**: 执行 `./scripts/deploy.sh` 完成对 `mini` 服务器的差量同步与服务热重载。

- **交付流水线更新 (v12.3.136)**:
    - **版本号对齐**: 同步更新 Client (`12.3.136`) 和 Server (`1.8.7`) 版本号。
    - **依赖修复**: 补全 `client` 缺失的 `jspdf` 和 `html2canvas` 宏包，执行 `npm install`。
    - **构建校验**: 完成本地 `npm run build` 验证，确保代码发布质量。
    - **快速部署**: 执行 `./scripts/deploy.sh` 完成对 `mini` 服务器的差量同步与服务热重载。

### 2026-03-07 (版本迭代与快速部署) (已由 2026-03-10 替代)
- **交付流水线更新 (v12.3.95)**:
    - **版本号对齐**: 同步更新 Client (`12.3.95`)、Server (`1.7.79`) 及根目录 (`1.5.47`) 版本号。
    - **构建校验**: 完成本地 `npm run build` 验证，确保代码发布质量。
    - **快速部署**: 执行 `./scripts/deploy.sh` 完成对 `mini` 服务器的差量同步与服务热重载。

### 2026-03-07 (版本迭代与全量部署)
- **交付流水线更新 (v12.3.84)**:
    - **版本号对齐**: 同补更新 Client 与 Server 的 Z 位版本号（v12.3.84 / v1.7.72）。
    - **构建校验**: 物理清理并重新构建 `dist` 目录。
    - **全量部署**: 通过 `./scripts/deploy.sh --full` 向 `mini` 服务器推送最新产物并完成远程安装依赖、编译构建与进程重载。

### 2026-03-06 (RMA 工作流修复与节点中文化)
- **RMA 流程修正与发版 (v12.3.83)**:
    - **金额保存逻辑修复**: 编辑了 `ActionBufferModal.tsx`，识别在 `ge_review` 阶段保存金额的必须性，避免工单被错误退回。
    - **全场景节点中文化**: 在前后端 (`tickets.js` 及 `UnifiedTicketDetail.tsx`) 中补齐节点映射图 (`NODE_NAME_MAP`)，令活动日志、后台消息、页脚栏位实现全面中文化的本地化阅读。
    - **线上部署**: 全链路执行完构建验证并依靠自动化流水线向 `mini` 投递了本次更新以保证线上产物的一致性。

### 2026-03-06 (版本迭代与全量同步)
- **交付流水线更新 (v12.3.82)**:
    - **版本号对齐**: 同步更新 Client 与 Server 的 Z 位版本号。
    - **构建校验**: 物理清理并重新构建 `dist` 目录。
    - **自动化部署**: 通过 `./scripts/deploy.sh` (Fast) 向 `mini` 服务器推送最新产物并完成进程重载。

### 2026-03-05 (工单附件 UI 增强与媒体预览系统)
- **附件详情侧滑窗 (v12.3.50)**:
    - **交互层级升级**: 实现了 `ActivityDetailDrawer` (400px)，支持点击时间轴条目滑出查看全文及附件。
    - **自适应媒体网格**: 取消了附件数量截断，引入响应式 Grid 布局平铺展示所有图片/视频缩略图。
    - **全能预览灯箱**: 重构 `MediaLightbox`，通过 `?inline=true` 参数支持图片大图预览与视频在线播放功能。
    - **上传安全校验**: 在评论组件中实装了 **50MB** 文件大小拦截逻辑，覆盖点击及拖拽上传场景。
    - **后端链路加固**: 修复了 `system.js` 后端分发路由的物理路径寻址、MIME 类型识别及 Inline 预览 Header 冲突。

### 2026-03-05 (审计模型健壮性与文档归档)
- **审计模型增强 (v12.3.44)**:
    - **对比逻辑进化**: 彻底解决了 Null/Empty 属性被误判为变更的 Bug。
    - **后端稳定性**: 解决了 SQLite 布尔值绑定异常和变量名引用错误。
    - **全量归档**: 补全了 `ticket_field_audit_log` 审计表的数据模型定义与 PATCH API 的分级权限说明。

### 2026-03-04 (UI 优化与时序修复)
- **UI 精细化与气泡通知 (v12.3.37)**:
    - **双重计数气泡**: 部门工单侧边栏现可同时展示“未读/待认领”和“活动总量”两个数字，更符合操作逻辑。
    - **Header 渲染策略**: 完成了顶栏统计组件在列表页与详情页的细粒度屏蔽，解决了 UI 堆叠和数据冗余。
    - **时序联动 Bug 修复**: 彻底修复了由于用户身份（actingDept）识别延迟导致的页面初始加载 0000 现象。
    - **全量部署**: 完成 v12.3.37 版本的生产环境分钟级自动化更新。

### 2026-03-04 (UI 精细化与功能抛光)
- **UI 一致性与体验增强 (v12.3.36)**:
    - **视觉对齐**: 实现了页面标题颜色与左侧菜单圆圈的动态同步（我的任务/协作/部门工单）。
    - **Tab 样式革新**: 将部门工单页面的选中 Tab 样式由实心黄色改为轻量的透明黄色背景加细边框，符合现代审美。
    - **SLA 信息直观化**: SLA 超时显示正式由“小时”切换为“天+小时”格式。
    - **安全确认机制**: 在部门工单“认领”操作中引入了基于 i18n 的确认对话框。
    - **版本部署**: 通过 `/upd` 完成 Client v12.3.36 的构建与线上分钟级部署。

### 2026-03-04 (生产环境部门名称一致性修复与权限修正)
- **生产环境部门名称标准化 (v12.3.34)**:
    - **部门名称标准化**: 引入 `normalizeDeptCode` 函数，兼容生产数据库中存储的中文部门名（“市场部”等），确保权限中间件能正确识别并下发 `MS`/`OP` 等短代码。
    - **修复 Overview 全零 Bug**: 解决了由于部门识别失败导致统计 API 返回空数据的缺陷。
    - **修复导航与 Tab 缺失 Bug**: 修正了导致非 Admin 角色无法看到 CRM 菜单以及部门工单 Tab 列表缺失的问题。
    - **数据修正**: 修改生产数据库中 `SherryFin` 的角色为 `Exec`，确保其拥有全局管理权限。

### 2026-03-04 (Overview 数据隔离与后端统计 API 落地)

- **部门视角的 Overview 系统上线 (v12.3.33)**:
    - **后端聚合接口**: 彻底重塑统计获取架构，引入专用下发 Endpoint（`team-stats`），有效屏蔽掉当前用户无权干预或跨部门的冗余工单流。
    - **前端精简解耦**: 剥离了 `ServiceOverviewPage.tsx` 中的长页面本地硬过滤机制，实现 O(1) 级别的 Dashboard 获取体验。
    - **ViewAs 多维透视**: 管理员启用 View As 视角模拟时，Dashboard 的各宏观负荷情况（Approvals / Risks）现在能够真正按被查验方部门生成数据透视。

### 2026-03-03 (客户生命周期与潜在客户管理)
- **客户生命周期闭环实装**:
    - **生命周期状态机**: 实装 `PROSPECT`, `ACTIVE`, `ARCHIVED` 状态模型及迁移脚本。
    - **访客转化流程**: 升级咨询工单转化逻辑，支持入库为“潜在个人/潜在机构”。
    - **自动升档逻辑**: 设备关联物主时自动将客户状态从 `PROSPECT` 提升至 `ACTIVE`。
    - **详情页操作增强**: 增加“升级为正式客户”快捷动作及全新的“更多”管理菜单（编辑/停用/删除/恢复）。

### 2026-03-01 (Workspace 线上 Bug 修复与稳定发版)

- **修复与部署 (v12.2.10)**:
    - **修复详情查询冲突**: 移除了工作区旧版独立的“查看完整详情”按钮以适应最新的 P2 原地侧开抽屉（内联详情）的完全体体验，避免旧 API 路由调用产生“工单不存在” 404 异常。
    - **重构视图 Title 渲染**: 纠正了硬编码图标的 Bug，让 `我的任务`、`协作`、`部门池` 三视图分别显示正确的多语言文字和对应的头部 Icon。
    - **权限对齐与深度发版**: 将预置资产的所属人归置到 Admin (ID:1) 名下，强行执行底层 Full 规则跑全量编译构建，推送至生产服务器后完全解包替换执行，修复了此前因局部缓存导致的客户端版号不同步。

### 2026-03-01 (Workspace UI 对齐与全量部署)
- **我的任务 (My Tasks) UI/UX 优化 (v12.2.8)**:
    - **表格布局与样式重构**: 将卡片完全改写为带有 `Star/Lock`, `ID`, `标题`, `状态`, `SLA 计时器` 表头的 MacOS 26 风格系统表格。工单 ID 字体白化放大，移除不必要的方向引导箭头。
    - **标题数据富化**: 融合了带有 VIP 标识的客户服务等级、客户名与问题简述。
    - **内联式沉浸体验**: 移除了原有的侧滑抽屉，变更为直接在本地展示完整的工单独立详情视图（左信息、右时间轴），并新增 macOS 原生的“返回”按键体验。
    - **操作优化与中文闭环**: Snooze 增加防护确认弹窗，补全了所有的前端多语言。并生成了 10 个横跨 VIP/RMA 场景的复杂测试工单部署下发。发布 v12.2.8 版本。

### 2026-03-01 (侧栏重构 + WorkspacePage 三视图)
- **侧栏菜单重构与 WorkspacePage 升级 (v12.2.2)**:
    - **侧栏重构**: 移除 MANAGEMENT/KNOWLEDGE/ADMIN section，工作区/工单/档案可折叠展开
    - **WorkspacePage**: 三视图（My Tasks/Mentioned/Team Queue），PRD 混合排序，Star/Snooze/Pick Up 交互
    - **i18n**: 四种语言 21 个新 key 补全
    - **部署验证**: v12.2.2 构建并部署，浏览器验证全部通过

### 2026-02-28 (P2 架构升级)
- **Service P2 架构升级代码实施 (v12.2.0)**:
    - **数据库层**: 创建统一工单表、活动时间轴表、通知表、序列管理表，编写数据迁移脚本
    - **SLA 引擎**: 实现时长矩阵、状态检测、超时计数、节点切换触发
    - **统一工单 API**: 单表多态 CRUD、工单升级转换、SLA 自动集成
    - **通知系统**: 前后端联动，支持 SLA 超时、@Mention、指派等多种通知类型
    - **前端组件**: 通知铃铛/面板、优先级/SLA状态/倒计时组件，全部 macOS26 毛玻璃风格

### 开发会话日志 (Development Session Log)

## [2026-03-04] 视觉抛亮与时序 Bug 修复 (v12.3.37)
### UI/UX
- **侧边栏气泡增强**: “部门工单”现在显示两个气泡：[全部活动工单(透明背景)] [待认领工单(黄色背景)]。
- **顶栏统计组件过滤**: 正确识别并屏蔽了咨询/RMA/经销商维修页面的全局统计顶栏，消除 UI 堆叠感。
- **视觉微调**: 修正了页面标题图标颜色，使其与侧边栏圆圈颜色完全一致。
- **多语言 SLA**: SLA 剩余时间在中文下现显示为“X天 Y小时”。
### Backend & Logic
- **API 扩展**: `/workspace/counts` 新增了 `team_hub_total` 和 `team_hub_unclaimed` 统计。
- **时序 Bug 修复**: 解决了从其它页面切回“部门工单”可能出现的 0000（状态水化）问题，通过将 `userDept` 加入 `useEffect` 依赖实现。
### Infrastructure
- **版本部署**: Client `12.3.37`, Server `1.7.30`。全量发布至 `mini`。

## [2026-03-04] UI 精细化修复与功能增强 (v12.3.36)
### UI/UX
- **标题视觉对齐**: 页面标题颜色现在根据当前视图动态匹配（我的任务-蓝色，协作-紫色，部门工单-橙色）。
- **Tab 样式微调**: 重新设计了部门工单 Tab 的选中态，使用 `rgba(255, 215, 0, 0.15)` 背景和黄色边框，提升专业感。
- **字体一致性**: 统一了工单 ID 的展示字体。
### Backend & Logic
- **SLA 格式化**: 实装了 SLA 剩余时间的“天-小时”展示逻辑。
- **认领确认**: 增加了工单认领的二次确认流程，防止误操作。
### Infrastructure
- **版本升级**: Client `12.3.36`, Server `1.7.29`。
- **构建修复**: 修复了 `WorkspacePage.tsx` 中重复的 `border` 属性导致的生产构建失败。

### 2026-02-27 (Session 11)
- **Tech Hub 布局精修与样式对齐 (v12.1.78)**:
    - **搜索框动态宽度**: 实现了搜索框宽度与搜索 Tab 的实时对齐逻辑。
    - **管理菜单交互更正**: 重定位管理按钮，增加箭头标识，并修复下拉菜单弹出方向。
    - **跨页面样式同步**: 统一了库存管理页与产品管理页的副标题视觉风格。
    - **代码库同步**: 完成全量 Git 分配与 Push。

### 2026-02-27 (Session 10)
- **Tech Hub 间距优化 (v12.1.77)**:
- **Tech Hub 视觉对齐与 Admin 设置抛光 (v12.1.75)**:
    - **全站品牌化正名**: 正式在全栈范围将“技术知识中心”简称为“Tech Hub”，消除了侧边栏及主页的多重称呼。
    - **Admin Setting 结构缩编**: 精简了历史中冗余的 System Health 等 Tab；全面重塑 Bokeh LLM 相关设置为一个连贯的入口，且迁移了部分基础策略设置至 General Settings。
    - **UI 打磨（iOS 26）**: 运用深色玻璃与半透明样式处理了说明文案，使其具备类原生应用的沉浸感。Tech Hub 搜索栏等微小布局间距完美对齐。
    - **多语言铺设**: 完成 CustomerManagement 页面的所有缺省词条国际化映射。
### 2026-02-27 (Session 8)
- **UI 品牌全量标准化对齐 (v12.1.64)**:
    - **品牌绿全局统一**: 收拢所有绿色变体至 Kine Green (#10B981)。
    - **AI 渐变 2.0**: Bokeh 进度条及图标更新为 Teal (#00BFA5) -> Lavender (#8E24AA)。
    - **成功反馈对齐**: Toast 成功通知颜色对齐。
    - **全站一致性**: 覆盖 Wiki、Service、Admin 及消息通知系统。

### 2026-02-27 (Session 7)
- **Bokeh 体验精修与导入优化 (v12.1.63)**:
    - **UI 精细化**: 将 Bokeh 优化进度文字设为白色，并引入 Kine Red (#EF4444) 风格的“取消优化”按钮。
    - **导入清洗增强**: 彻底移除 Docx/URL 导入时内容开头的冗余重复标题。
    - **品牌一致性**: 知识库导入器的 “Bokeh 优化” 按钮背景更新为品牌渐变色（绿-紫）。
    - **全量部署**: 集成版本递增与远程部署流水线。

### 2026-02-27 (Session 6)
- **UI/UX 专项优化与全量部署 (v1.5.37)**:
    - **Bokeh AI 视觉升级**: 为智能回答标题添加品牌渐变色，并实现条件可见性。
    - **Header 结构重构**: 通过三栏布局解决 Wiki 编辑器标题重叠 Bug，确保摘要按钮居中。
    - **Kine Red 警示系统**: 全局确认框支持危险操作自动变红，强化删除动作的安全感。
    - **Admin 模块稳定性**: 修复设置页 Tab 回跳及加载闪烁，确保 TopBar 状态实时同步。

- **Wiki 导入评论区清理与 Bokeh 术语优化 (v1.5.31)**:
    - **评论区深度清理**: 重构 `removeContentTitle` 函数，引入分层截断逻辑，解决短正文长评论页面的截断失效问题。
    - **术语映射**: 明确 Eagle -> 猎影 的翻译对照，并完善内容清洁规则。
    - **数据修复**: 手动修复了 Article 756 的评论区污染。

### 2026-02-26 (Session 4.5)
- **Wiki 导入深层清洗与 UI 优化 (v1.5.30)**:
    - **Scoping Fix**: 修复了 `removeContentTitle` 作用域问题，使翻译内容能正确经过清洗程序。
    - **UI 垂直化**: 将版本号显示改为双行垂直布局。
    - **功能完善**: 剥离面包屑导航及 Previously/Next 尾部噪音。

### 2026-02-24 (Session 4)
- **知识库导入优化与网页抓取修复 (v1.3.4)**:
    - **网页抓取引擎升级**: 实现了基于 H1/H2 的自动章节切分转换逻辑，支持 Turbo (Markdown) 和 Standard (HTML) 模式下的长网页结构化导入。
    - **UI 视觉细节调优**: 修复了产品型号选中后的文字颜色对比度问题（灰色 -> 白色）。
    - **进度交互优化**: 根据导入模式动态切换进度文字（Hidden: Upload step for URL import）。
    - **品牌一致性**: 将导入完成按钮颜色统合为 Kine Green (#00A650)。
    - **内容渲染净化**: 后端自动剥离硬编码的白底样式，确保网页内容在深窗主题下的融合度。

### 2026-02-24 (Session 3)
- **详情页布局与导航模块化标准化 (v12.1.31)**:
    - **黑屏问题根治**: 识别并修复了 `Inquiry`, `RMA`, `DealerRepair` 详情页中的 `100vh` 布局冲突，彻底解决了嵌套于 `MainLayout` 时出现的显示异常。
    - **全站单页导航对齐**: 将 Wiki 系统的外部跳转逻辑统一收敛为 SPA 内部 `navigate` 模式，规避了状态丢失风险。
    - **资产联动闭环**: 补全了客户详情页中设备卡片的点击事件，实现了直接关联至技术支持库（Wiki）的功能。

### 2026-02-24 (Session 2)
- **CustomerDetailPage 响应式与逻辑优化 (v12.1.30)**:
    - **自适应布局组件化**: 成功应用 `auto-fill` 网格策略于 Dashboard 和设备资产模块，解决了视窗缩放时的卡片溢出。
    - **全量 UI 汉化**: 剥离了 Dashboard 统计项与设备资产分类中的硬编码字符，并利用 `tc()` 助手修复了 TS 校验报错。
    - **SVC 场景化交互**: 实现了维修工单在 Dealer/Customer 页面下的“对侧实体”展示逻辑，信息流更加清晰。
    - **CRM 核心逻辑加固**: 修复了 `contacts.js` 在处理主要联系人状态切换时的覆盖 Bug。
    - **设备卡片视觉重构**: 上线了横向 Pill 样式的 `ProductCard`。

### 2026-02-24 (Session 1)
- **Bokeh 知识检索与格式化修复 (v12.1.28)**:
    - 修复 `ai_service.js` 中知识库检索的 FTS 表名及 `LIKE` 兜底丢失问题。
    - 将 AI 工单 Prompt 上下文补齐 `contact_name`。
    - 修复了工单卡片联系人的前缀冗余与分隔符（从 `·` 改为 `|`）。
- **Bokeh UI 与搜索深度优化**:
    - **知识库 RAG 增强**: 将知识库检索逻辑从严格匹配优化为宽泛关键词匹配，解决 AI 无法拉取相关知识的问题。
    - **交互链路优化**: 对话卡片（工单/文章）点击改为新标签页打开，移除本地 Dialog 冗余代码。
    - **工单显示精简**: 实现了工单卡片的姓名去重逻辑，确保 SVC 工单与普通工单信息展示更专业。
    - **自动化发布**: 成功执行 V12.1.27 版本的自动化构建与远程部署。
- **Bokeh UI 与搜索优化**:
    - 将用户消息气泡背景改为 Kine Green (`#4CAF50`)，助手气泡改为灰色。
    - 修复了 SQL 拼接逻辑中缺失 `1=1` 导致的搜索稳健性问题。
    - 优化了技术词汇（SDI/HDMI）在 FTS5 环境下的命中率。
    - 针对 SVC- 工单，实现了经销商账户名称的精准显示与“经销商:”标识。
- **知识导入工具深度优化**: 
    - 修复了 URL 导入 (Turbo 模式) 的变量作用域 Bug。
    - 实现了产品型号的**多选标签**交互。
    - 重构进度面板摘要为叙述性文字描述（macOS26 风格）。

### 2026-02-23
- **Admin 设置体验升级**: 将所有管理端原生 `alert` 替换为响应式 `toast` 通知（Kine Yellow 风格）。
- **每日一词可见性修复**: 取消了公共设置接口 `/public-settings` 的鉴权要求。
- **Wiki 导入样式统一**: 细化了知识生成器中“Bokeh 优化”按钮的选中态。
- **搜索 Tab 视觉分离**: 利用 `flex: 1` 占位符将搜索历史 Tab 强制推至 Tab 栏的最右侧。
- **Admin 设置状态持久化**: 实现了 Admin 面板的二级路由记忆功能。
- **全量设置存入 DB**: 彻底迁移了「显示每日一词徽章」等配置至 `system_settings` 数据库。

### 2026-02-22
- **知识库UI对齐完善**: 删除了 `1.1.1` 冗余前缀章节号渲染 Bug，重构了 Manual TOC Modal。
- **知识库导入与本地服务修复**: 解决了因硬编码 `/Volumes/fileserver` 导致的 Node 崩溃。

### 2026-02-21
- **搜索质量提升**: 实现多关键词 AND 拆分搜索。
- **全量字段对齐**: 完成全栈 `customer_id` -> `account_id` 迁移。
- **搜索范围重大突破**: 彻底解除“仅搜已关闭工单”限制。
- **知识库 (Phase 2) 标记完成**: 全方位文档同步 (PRD, API, DataModel, Scenarios)。

---

## 📋 待办任务

### [DEALER-001] 经销商停用功能完善
**状态**: ⬜ 待开发  
**优先级**: P2  
**计划**:
1. 停用时禁止关联用户登录
2. 停用前检查API（展示风险项）
3. 库存处理策略（冻结/转移）

### [SYSTEM-001] 全局权限与安全审计
- 细化 `Employee` 与 `Market` 角色的权限边界。
- 完善所有数据导出操作的审计日志。

---

## ✅ 已完成任务 (历史记录)

### [WIKI-005] 评论区清理与 Bokeh 术语优化 (2026-02-26)
### [WIKI-004] 知识库深层清洗与 AI 翻译修复 (2026-02-26)
### [WIKI-003] 知识库网页导入优化 (2026-02-26)
### [WIKI-001] Wiki 主界面重新设计 + Bokeh AI 融合 (2026-02-15)
### [WIKI-002] 知识导入交互与稳定性改进 (2026-02-24)

---

## 任务编号规范
- `WIKI-xxx`: Wiki/知识库相关
- `TICKET-xxx`: 工单系统相关
- `BOKEH-xxx`: Bokeh AI 相关
- `UI-xxx`: 界面设计相关
- `API-xxx`: 后端接口相关
- `INV-xxx`: 库存管理相关
- `DEALER-xxx`: 经销商管理相关
